{
  "name": "webhook-cms-pull",
  "version": "1.0.0",
  "description": "Pull data from external sources into a Webhook CMS Firebase.",
  "main": "sync.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preinstall": "cd elastic-search && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "clone": "^1.0.2",
    "debug": "^3.1.0",
    "firebase": "^2.2.3",
    "html-template": "^1.2.2",
    "knox": "^0.9.2",
    "moment": "^2.9.0",
    "moment-timezone": "^0.4.0",
    "pump": "^3.0.0",
    "run-parallel-limit": "^1.0.5",
    "through2": "^2.0.3"
  }
}
